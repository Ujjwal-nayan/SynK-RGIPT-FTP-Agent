# SynK - RGIPT FTP Agent Requirements (Linux)
# Python 3.8+ required

# GTK bindings (install via system package manager)
# Ubuntu/Debian: sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-3.0
# Fedora/RHEL: sudo dnf install python3-gobject gtk3
# Arch: sudo pacman -S python-gobject gtk3

# No additional pip packages required - uses Python standard library:
# - ftplib (built-in)
# - json (built-in)
# - threading (built-in)
# - pathlib (built-in)
# - hashlib (built-in)
